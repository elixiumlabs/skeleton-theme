{% liquid
  assign featured_collection = section.settings.featured_collection
  assign hero_image = section.settings.hero_image
  assign hero_focal = ''
  if hero_image.presentation.focal_point
    assign hero_focal = 'object-position: ' | append: hero_image.presentation.focal_point | append: ';'
  endif
%}

<section class="lash-home full-width">

  {{- '<!-- HERO -->' -}}
  <div class="lash-hero">
    {% if hero_image != blank %}
      {{
        hero_image
        | image_url: width: 2400
        | image_tag:
          class: 'lash-hero__image',
          widths: '750, 1100, 1500, 2200, 2400',
          sizes: '100vw',
          loading: 'eager',
          fetchpriority: 'high',
          style: hero_focal,
          alt: section.settings.heading
      }}
    {% else %}
      <img
        class="lash-hero__image"
        src="{{ 'lash-candii-hero-gloss.png' | asset_url }}"
        alt="{{ section.settings.heading | escape }}"
        width="1792"
        height="1024"
        loading="eager"
        fetchpriority="high"
      >
    {% endif %}

    <div class="lash-hero__overlay"></div>

    <div class="lash-hero__content">
      <p class="eyebrow">{{ section.settings.eyebrow }}</p>
      <h1>{{ section.settings.heading }}</h1>
      <p class="lash-hero__copy">{{ section.settings.copy }}</p>

      <div class="lash-hero__actions">
        <a class="button" href="{{ section.settings.primary_link | default: routes.all_products_collection_url }}">
          {{ section.settings.primary_label }}
        </a>
        <a class="button button--secondary" href="{{ section.settings.secondary_link | default: '/pages/about' }}">
          {{ section.settings.secondary_label }}
        </a>
      </div>
    </div>
  </div>

  {{- '<!-- MARQUEE MOODBAR -->' -}}
  <div class="lash-moodbar" aria-label="Brand highlights">
    <div class="lash-moodbar__track">
      <span>Gloss Finish</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span>Baby-Pink Energy</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span>After-Dark Volume</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span>Soft Glam Daily</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span>Luxury Lashes</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span>Main Character</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">Gloss Finish</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">Baby-Pink Energy</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">After-Dark Volume</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">Soft Glam Daily</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">Luxury Lashes</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
      <span aria-hidden="true">Main Character</span>
      <span class="lash-moodbar__sep" aria-hidden="true">✦</span>
    </div>
  </div>

  {{- '<!-- COLLECTION INTRO -->' -}}
  <div class="lash-home__intro">
    <p class="eyebrow">{{ section.settings.collection_eyebrow }}</p>
    <h2>{{ section.settings.collection_heading }}</h2>
    <p>{{ section.settings.collection_copy }}</p>
  </div>

  {{- '<!-- FEATURED PRODUCTS -->' -}}
  <div class="lash-products product-grid">
    {% if featured_collection != blank and featured_collection.products_count > 0 %}
      {% for product in featured_collection.products limit: 4 %}
        {% render 'product-card', product: product %}
      {% endfor %}
    {% else %}
      {% assign fallback_products = 'Classic Wisp,Velvet Volume,Baby Doll Lift,After Dark Luxe' | split: ',' %}
      {% for item in fallback_products %}
        <div class="lash-product lash-product--placeholder">
          <span class="lash-product__media">
            <span class="lash-product__placeholder"></span>
          </span>
          <span class="lash-product__content">
            <span>{{ item }}</span>
            <span class="price">$24.00</span>
          </span>
        </div>
      {% endfor %}
    {% endif %}
  </div>

  {{- '<!-- LASH RITUAL -->' -}}
  <div class="lash-ritual">
    <div class="lash-ritual__header">
      <p class="eyebrow">{{ section.settings.ritual_eyebrow }}</p>
      <h2>{{ section.settings.ritual_heading }}</h2>
    </div>
    <div class="lash-ritual__steps">
      <div>
        <span class="lash-ritual__num">01</span>
        <p>{{ section.settings.ritual_step_1 }}</p>
      </div>
      <div>
        <span class="lash-ritual__num">02</span>
        <p>{{ section.settings.ritual_step_2 }}</p>
      </div>
      <div>
        <span class="lash-ritual__num">03</span>
        <p>{{ section.settings.ritual_step_3 }}</p>
      </div>
    </div>
  </div>

</section>

{% stylesheet %}
  @keyframes lash-marquee {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  .lash-home {
    background: var(--color-background);
    display: grid;
    grid-template-columns: var(--content-grid);
  }

  .lash-home > * {
    grid-column: 2;
  }

  /* ─── HERO ─── */
  .lash-hero {
    color: #F2E8DC;
    display: grid;
    grid-column: 1 / -1;
    min-height: clamp(42rem, 90svh, 60rem);
    overflow: hidden;
    position: relative;
  }

  .lash-hero__image,
  .lash-hero__overlay,
  .lash-hero__content {
    grid-area: 1 / 1;
  }

  .lash-hero__image {
    height: 100%;
    object-fit: cover;
    object-position: center;
    width: 100%;
  }

  /* Deep editorial dark overlay — Fenty/Kylie style */
  .lash-hero__overlay {
    background:
      linear-gradient(105deg,
        rgba(11, 8, 16, 0.92) 0%,
        rgba(11, 8, 16, 0.72) 38%,
        rgba(11, 8, 16, 0.22) 62%,
        rgba(11, 8, 16, 0.06) 100%),
      linear-gradient(0deg,
        rgba(11, 8, 16, 0.55) 0%,
        rgba(11, 8, 16, 0) 35%);
  }

  .lash-hero__content {
    align-content: center;
    display: grid;
    gap: 1.5rem;
    max-width: min(54rem, calc(100% - var(--page-margin) * 2));
    padding: 5rem var(--page-margin);
    width: 100%;
  }

  /* Oversized serif headline — the money shot */
  .lash-hero h1 {
    font-size: clamp(4.2rem, 11vw, 12rem);
    font-weight: 400;
    letter-spacing: 0.03em;
    line-height: 0.9;
    max-width: 10ch;
    text-transform: uppercase;
  }

  .lash-hero__copy {
    color: rgba(242, 232, 220, 0.68);
    font-size: 0.95rem;
    letter-spacing: 0.04em;
    line-height: 1.75;
    max-width: 34rem;
  }

  .lash-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 0.35rem;
  }

  /* ─── MARQUEE MOODBAR ─── */
  .lash-moodbar {
    background: #0F0B14;
    border-top: 1px solid rgba(201, 168, 124, 0.1);
    border-bottom: 1px solid rgba(201, 168, 124, 0.1);
    grid-column: 1 / -1;
    overflow: hidden;
  }

  .lash-moodbar__track {
    align-items: center;
    animation: lash-marquee 32s linear infinite;
    display: flex;
    width: max-content;
  }

  .lash-moodbar span {
    color: rgba(242, 232, 220, 0.45);
    font-family: var(--font-primary--family);
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.25em;
    padding: 1.15rem 2rem;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .lash-moodbar__sep {
    color: var(--color-accent);
    font-size: 0.45rem;
    opacity: 0.7;
    padding: 0 0;
  }

  /* ─── COLLECTION INTRO ─── */
  .lash-home__intro {
    margin-block: 6.5rem 2.75rem;
    max-width: 58rem;
  }

  .lash-home__intro h2 {
    font-size: clamp(2.6rem, 6vw, 6.5rem);
    font-weight: 400;
    letter-spacing: 0.04em;
    margin-top: 1.25rem;
    text-transform: uppercase;
  }

  .lash-home__intro > p:not(.eyebrow) {
    color: rgba(242, 232, 220, 0.55);
    font-size: 0.92rem;
    letter-spacing: 0.04em;
    line-height: 1.75;
    margin-top: 1.35rem;
    max-width: 44rem;
  }

  /* ─── PRODUCT GRID (placeholder cards) ─── */
  .lash-products {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .lash-product {
    background: #120D18;
    color: var(--color-foreground);
    display: grid;
    min-width: 0;
    text-decoration: none;
    transition: background 260ms ease;
  }

  .lash-product:hover {
    background: #180F20;
  }

  .lash-product__media {
    aspect-ratio: 4 / 5;
    background: #1C1426;
    display: block;
    overflow: hidden;
  }

  .lash-product__image {
    height: 100%;
    object-fit: cover;
    transition: transform 360ms ease;
    width: 100%;
  }

  .lash-product:hover .lash-product__image {
    transform: scale(1.045);
  }

  .lash-product__placeholder {
    background: linear-gradient(145deg, rgba(201, 168, 124, 0.09), rgba(11, 8, 16, 0.65));
    display: block;
    height: 100%;
    width: 100%;
  }

  .lash-product__content {
    align-items: center;
    display: flex;
    gap: 1rem;
    justify-content: space-between;
    min-height: 5rem;
    padding: 1.1rem 1rem;
  }

  .lash-product__content span:first-child {
    color: var(--color-foreground);
    font-family: var(--font-primary--family);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  /* ─── RITUAL SECTION ─── */
  .lash-ritual {
    align-items: start;
    background: #0D0914;
    border: 1px solid rgba(201, 168, 124, 0.1);
    display: grid;
    gap: 2.5rem;
    grid-template-columns: minmax(16rem, 0.7fr) 1fr;
    margin-top: 6rem;
    margin-bottom: 6rem;
    padding: clamp(2rem, 4vw, 3.75rem);
  }

  .lash-ritual__header h2 {
    font-size: clamp(2.2rem, 4.5vw, 5rem);
    font-weight: 400;
    letter-spacing: 0.04em;
    margin-top: 1.1rem;
    text-transform: uppercase;
  }

  .lash-ritual__steps {
    background: rgba(201, 168, 124, 0.06);
    display: grid;
    gap: 1px;
    grid-template-columns: repeat(3, 1fr);
  }

  .lash-ritual__steps div {
    background: #0D0914;
    min-height: 12rem;
    padding: 1.5rem;
    transition: background 220ms ease;
  }

  .lash-ritual__steps div:hover {
    background: #120E1A;
  }

  .lash-ritual__num {
    color: var(--color-accent);
    display: block;
    font-family: var(--font-primary--family);
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.22em;
  }

  .lash-ritual__steps p {
    color: rgba(242, 232, 220, 0.72);
    font-family: var(--font-secondary--family);
    font-size: clamp(1.1rem, 2vw, 1.7rem);
    font-weight: 400;
    letter-spacing: 0.03em;
    line-height: 1.2;
    margin-top: 1.5rem;
  }

  /* ─── RESPONSIVE ─── */
  @media (max-width: 990px) {
    .lash-ritual {
      grid-template-columns: 1fr;
    }

    .lash-products {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .lash-hero {
      min-height: 46rem;
    }

    .lash-hero__image {
      object-position: 65% center;
    }

    .lash-hero__overlay {
      background:
        linear-gradient(180deg,
          rgba(11, 8, 16, 0.88) 0%,
          rgba(11, 8, 16, 0.62) 55%,
          rgba(11, 8, 16, 0.18) 100%);
    }

    .lash-hero__content {
      align-content: start;
      padding-top: 5rem;
    }

    .lash-hero h1 {
      font-size: 3.9rem;
    }

    .lash-ritual__steps {
      grid-template-columns: 1fr;
    }

    .lash-products {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .lash-moodbar__track {
      animation: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Luxury lash homepage",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Hero eyebrow",
      "default": "Gloss-drip lash studio"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Hero heading",
      "default": "Lash Candii"
    },
    {
      "type": "textarea",
      "id": "copy",
      "label": "Hero copy",
      "default": "High-impact lashes with a glossy, girly finish: soft enough for daytime, bold enough for every main-character moment."
    },
    {
      "type": "text",
      "id": "primary_label",
      "label": "Primary button label",
      "default": "Shop lashes"
    },
    {
      "type": "url",
      "id": "primary_link",
      "label": "Primary button link"
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "Secondary button label",
      "default": "Find your vibe"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Secondary button link"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Featured collection"
    },
    {
      "type": "text",
      "id": "collection_eyebrow",
      "label": "Collection eyebrow",
      "default": "The glam edit"
    },
    {
      "type": "text",
      "id": "collection_heading",
      "label": "Collection heading",
      "default": "Lashes that look expensive before you even blink."
    },
    {
      "type": "textarea",
      "id": "collection_copy",
      "label": "Collection copy",
      "default": "Curated sets for clean-girl lift, flirty pink gloss, and full-volume nights out."
    },
    {
      "type": "text",
      "id": "ritual_eyebrow",
      "label": "Ritual eyebrow",
      "default": "Lash ritual"
    },
    {
      "type": "text",
      "id": "ritual_heading",
      "label": "Ritual heading",
      "default": "Your lash routine, polished."
    },
    {
      "type": "text",
      "id": "ritual_step_1",
      "label": "Step 1",
      "default": "Measure and trim from the outer corner for a custom fit."
    },
    {
      "type": "text",
      "id": "ritual_step_2",
      "label": "Step 2",
      "default": "Let adhesive turn tacky before pressing along the lash line."
    },
    {
      "type": "text",
      "id": "ritual_step_3",
      "label": "Step 3",
      "default": "Lift, blend, and store the lash back in its tray."
    }
  ],
  "presets": [
    {
      "name": "Luxury lash homepage",
      "category": "Image"
    }
  ]
}
{% endschema %}
